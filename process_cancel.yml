---
    - name: "Playing with Ansible and Git"
      hosts: localhost
      connection: local
      shell: hostname
      tasks:
        - include: tasks/getprocess.yml
      
        - include: tasks/terminateproc.yml